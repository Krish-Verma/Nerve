# Nerve Roadmap

Authoritative slice list. Update the status column at the end of every slice.
**Never begin a slice without explicit approval.**

| # | Slice | Status |
|---|---|---|
| 1 | Indexing foundation — `init`/`index`/`status`/`search`, SQLite evidence schema, TS/JS entities, `CONTAINS`/`DEFINES`/`IMPORTS`/`EXPORTS` | ✅ Complete (2026-07-31) — 137 tests pass, ADR-0001 gate passed |
| 2a | Static relationship resolution — `CALLS`, `REFERENCES`, `EXTENDS`, `IMPLEMENTS`, lexical binding + import resolution, negative fixtures + measured precision | ✅ Complete (2026-07-31) — 203 tests pass, FP=0 FN=0 on 24 resolved edges |
| 2b | Graph query surface — `nerve path`, `nerve why`, evidence packets | ✅ Complete (2026-07-31) — 261 tests pass, query-time path safety verified by attack |
| 3 | Incremental indexing — changed-file detection, import-closure invalidation, **deletion**, `IdentityLink`, full-vs-incremental equivalence | ✅ Complete (2026-07-31) — 295 tests, equivalence holds over 24 seeded edits; **speed target missed (24.9% vs 20%)** |
| 3b | Normalize repository state out of `occurrence`/`observation` and out of `occurrence_id` — removes the O(repository) restatement pass | ✅ Complete (2026-07-31) — 306 tests; **24.9% → 2.0%**; counted-writes gate; found and fixed a silent data-destruction bug |
| 4a | `nerve serve` — loopback HTTP, read-only JSON API, T4/T5/T6 security controls | ✅ Complete (2026-07-31) — 427 tests; token/origin/host/traversal/symlink/XSS all attack-verified |
| 4b | `apps/nerve-web` — the visual explorer SPA, asset embedding, screenshot QA | ✅ Complete (2026-07-31) — 435 tests, 31 screenshots reviewed, 0 CSP violations; **fixed a 4a bug that made the UI unloadable** |
| 5a | Markdown + ADR ingestion — `Document`/`Section` entities, `CONTAINS` structure, ADR status, T7 controls | ✅ Complete (2026-08-01) — 506 tests; T7 exhaustive and mutation-verified; **closed two identity-forgery vectors**, one of them pre-existing in every id constructor |
| 5b | Markdown **link scanning** — destinations, forms, spans; code-span mentions counted not emitted | ✅ Complete (2026-08-01) — 541 tests; the tests found 6 real scanner defects, two of them turning hostile HTML into link destinations |
| 5c | Document↔code **link resolution** — `REFERENCES` by explicit path and `#L<n>` anchor, unresolved reasons, measured precision, invalidation | ✅ Complete (2026-08-01) — 564 tests; FP=0 on fixtures, but **only 5 link sites exist across Nerve's own 45 documents** |
| 5d-i | **Corrective** filesystem evidence — `FILESYSTEM_OBSERVED` + `fs-structural`, ADR-0007, schema v4 migration, amended T7 | ✅ Complete (2026-08-01) — 577 tests; a docs-only tree now yields **0** `ts-js-structural` observations, was 4 mislabelled `AST_DIRECT` |
| 5d-ii | `Document SUPERSEDES Document` — explicit evidence only, chains, cycles, ambiguity, measured precision | ✅ Complete (2026-08-01) — 596 tests; FP=0 over a 26-file corpus whose ground truth was written before the resolver; **Nerve's own ADRs state no supersession and produce 0 edges** |
| 5d-iii | UI vocabulary catch-up — glosses driven from the Rust vocabularies, asset re-embed, screenshots | ✅ Complete (2026-08-01) — 610 tests; a new test reads the TypeScript gloss maps and fails when a Rust vocabulary gains a member, so the two cannot drift again; **found 120 real sites rendering fallback text**, and a gloss for a status the backend cannot emit |
| 6a | Coverage **vocabulary + LCOV parser** — `CoverageRun`, `COVERS`, ADR-0008 | ✅ Complete (2026-08-01) — 648 tests; the gating question answered empirically: **LCOV is aggregate**, so the endpoint is a coverage run and `TEST_COVERS_SYMBOL` is refused by the vocabulary |
| 6b | Coverage **ingestion** — `nerve coverage`, line→symbol mapping, freshness, T9 attack tests, equivalence | ✅ Complete (2026-08-02) — 692 tests; T9 verified by attack; **zero call-shaped relations asserted over `Relation::ALL`**; found that coverage surviving a re-index required splitting the pipeline's withdrawal in two, and that a file changed since indexing must be refused rather than mapped through stale extents |
| 7a | `nerve gaps` — the coverage-gap question, CLI + API | ✅ Complete (2026-08-02) — 729 tests; **"no coverage ingested" is a distinct, unanswerable state with `totals: null`**, not a list of every symbol; four states incl. `unmeasured`; CLI and API asserted byte-equal; **fixed a harness defect where a failing server test hung the whole suite instead of failing** |
| 7a-ii | **Corrective** — "Gaps" meant two things across surfaces; SPA view renamed **Unresolved**, new **Coverage** view over `/api/gaps` | ✅ Complete (2026-08-02) — 729 tests, frontend 15 tests, lint clean, assets re-embedded; **screenshot QA performed** (the earlier failure was two Chrome browsers connected at once) — absent/uncovered/unmeasured/partial/stale all verified live, 0 console messages; **narrow-viewport QA still outstanding** (window ignores resize) |
| 7a-iii | **Corrective** — the rail labels `entities_total` as "Symbols". Pre-existing from 4b, made visible by 7a-ii | ✅ Complete (2026-08-02) — 735 tests; canonical `symbols_total` from `EntityKind::is_symbol()` on `/api/overview`, `status --json`, `index --json` and both text outputs; **the symbol-kind SQL list had been generated in three separate places and is now one helper with four call sites**; the invariant is asserted over *every* non-symbol kind, not an example, and all twelve kinds are individually pinned so a new kind cannot default silently; orchestrator mutation probe (`coverage_run` made a symbol) failed **16 tests across 6 targets** |
| 7b | `nerve impact` — reverse dependency closure with evidence | ✅ Complete (2026-08-02) — 771 tests; BFS reverse closure over `CALLS`/`REFERENCES`/`EXTENDS`/`IMPLEMENTS` reusing `graph.rs` adjacency, exact tallies beneath a row cap, cycles terminate with each entity once; **the unresolved account is a field on every answer, printed and serialized even when zero** — on `ts-basic`, `add` has 3 dependants beside 4 unresolved sites; `CONTAINS`/`DEFINES`/`IMPORTS`/`COVERS` excluded from the default with a reason each; `/api/impact` is the 11th route; **the implementation agent was killed mid-slice by an org spend limit and the orchestrator finished the API surface and all 12 CLI/API tests directly** |
| 7c-i | `nerve check` — the CI verdict and its exit codes | ✅ Complete (2026-08-02) — 795 tests; five verdicts over `index_freshness` + `is_healthy()`, one new exit code `STALE_INDEX = 4`, `Unverified` distinct from `Stale` so a truncated sweep is never a clean bill; **the brief was wrong and the implementer proved it with a test** — `index_freshness` iterates `module_facts`, so an *added* file is invisible to it, and `untracked_files` was needed; read-only enforced by `query_only=ON` and verified on the bytes; orchestrator confirmed **zero false positives across all 7 fixtures** and that unsupported file types do not trigger staleness |
| 7c-ii | `nerve doctor` — diagnostics | ✅ Complete (2026-08-02) — 821 tests; 11 checks, one finding per check every run, a check that could not run is `skipped` **with its cause** rather than omitted or reported as passing; closed id vocabulary pinned by two tests; no new exit code (fatal reuses `NO_INDEX`, whose documented meaning already covers it); **`SELECT count(*) FROM entity_fts` was found by probe to read the content table and so can never detect FTS drift** — the `entity_fts_docsize` shadow table is used instead; the no-SQL-in-the-CLI guard was widened from `main.rs` to the whole crate rather than evaded; orchestrator verified no panic on a zero-byte database, a `.nerve` that is a file, a `nerve.db` that is a directory, and a `nerve.db` symlinked to `/etc/passwd` with no content disclosed |
| 8a | MCP — stdio JSON-RPC, `nerve_investigate`, **T7 + T8 gates** | ✅ Complete (2026-08-02) — 862 tests; **zero new crates** (framing hand-rolled on `serde_json`; the async-runtime trade was already measured in 4a and not reopened); every repository-derived value confined to one `repository_content` field and held there by a property test that walks the whole response; three independent response bounds incl. a byte ceiling measured on the text a client actually reads, with exact continuation and the degenerate case named; a traversal-shaped selector refused **as a refusal**, never disguised as "not found"; orchestrator injected a hostile Markdown **heading** and found all 7 occurrences inside labelled regions, 0 leaks |
| 8b-i | **Selector resolution by entity kind** — path names what is at it, qualifiers, shared traversal refusal | ✅ Complete (2026-08-02) — 911 tests; measured first: **26% of entities in `fixtures/md-docs` could not be named by their path** and the failure message suggested strings that resolve to nothing; `EntityKind::path_role()` puts the classification in the vocabulary with all twelve kinds pinned, so a new kind cannot be silently unaddressable; qualifiers are **generated** from `EntityKind::as_str()` rather than hand-listed, the drift 5d-iii and 7a-iii were corrective slices for; a path with two readings resolves by a stated rule and **reports what it passed over** rather than choosing silently, while two readings *inside* one tier stay ambiguous; the traversal refusal became one helper for all three surfaces — **CLI and HTTP previously answered "matches no indexed entity" to `../../etc/passwd`, asserting a check they never ran**; two private copies of `qualified_name` deleted; **two defects inherited from 8a found in review** — `./x` was refused as an escape, `..\..\x` was not refused at all; orchestrator ran all four mutation probes (19/21/8/5 failures) after the implementation agent was interrupted twice by infrastructure limits |
| 8b-ii | MCP — the rest of the tool surface (`search`, `path`, `impact`, `gaps`) | ✅ Complete (2026-08-02) — 970 tests; **five tools, and a sixth candidate dropped because 8b-i removed its reason** (document evidence is now reachable through `nerve_investigate`, so a document tool would be `investigate` with a kind filter); 8a's envelope **extracted into `mcp/tool.rs` rather than copied four times**, every 8a test kept verbatim; `nerve-store`, `nerve-core`, `nerve-index` and `api.rs` **untouched** — the tools are pure surface over the application layer the CLI and HTTP already call; the T7 property test extended to all five with **two anti-vacuity assertions built into the helper**, closing the trap that produced a false pass twice on this project; `gaps` keeps `totals: null` distinct from `gaps: 0` on three independent signals; **the implementer reported that exact continuation is unavailable without a new store function and declined to invent paging on one surface** — `next_offset: null` and a statement, rather than a cursor no query honours; orchestrator's fifth probe (disable the shared traversal refusal) failed **7 tests with one per tool**, proving criterion 7 is tested per tool and not once |
| 9a | Python **structure** — `py-structural`, modules/packages/imports/functions/classes/methods | ✅ Complete (2026-08-02) — 1012 tests; **one new crate** (`tree-sitter-python 0.25.0`, MIT, 100 → 101 measured), and `tree-sitter-stack-graphs-python` **rejected on clean-room grounds** because a grammar is a parser and stack-graphs is a competing name-resolution engine; **zero `ts-js-*` observations in a Python repository**, the 5d-i invariant restated for a new language and verified on the real database; **an import site makes two separable claims** — `from pkg.util import *` keeps the true `module → pkg/util.py` edge *and* records `wildcard:pkg.util` as unknowable, rather than deleting a true fact to express an unrelated doubt; ground truth written before the resolver and **the fixture won a disagreement** about whether a dynamic import also raises a conditional finding; the implementer ran an unrequested third probe and **found a never-firing test** — the `forbidden` list was unreachable behind a set-equality assertion; orchestrator attacked T1 directly (hostile `setup.py` + top-level `os.system`): **0 markers, 9 entities indexed**, so the check is not vacuous; TS/JS precision re-run and unchanged at FP=0 FN=0 |
| 9b | Python **references** — `py-reference`, `CALLS`/`REFERENCES`/`EXTENDS`, separate precision gate | ✅ Complete (2026-08-03) — 1058 tests; **two languages, two tables, no combined number** — Python **FP=0, FN=3 declared**, CALLS 42.3% unresolved / 32.4% overall, TS/JS unchanged at FP=0 FN=0 38.1%; the FNs are *pinned as must-stay-absent*, so an FN cannot be "fixed" by guessing without deliberately promoting it; **`self.method()` is unresolved and that is correct** — knowing `self` is an `Engine` does not say what `self.start()` calls, because a subclass instance is what is usually passed, so resolving it would be a false positive rather than an over-approximation (TypeScript's `this` is a language binding; Python's `self` is a parameter name); unmodelled call/base-class sites **counted by form and asserted as gate 7**, so the precision denominator is auditable; **an upgrade-path bug found that no test could catch** — the Python cache key would have matched a 9a-era index exactly, silently producing zero reference edges forever, verified by the orchestrator simulating a 9a index and confirming the cache miss; four Python scoping rules modelled incl. whole-function-body binding and class-body-is-not-an-enclosing-scope; orchestrator's third probe (make a class body enclosing) fails 2 targets; **the implementation agent was killed mid-slice by an org monthly spend limit, the second time on this project**, and was resumed from its transcript rather than restarted |
| 10a | **Framework rules — HTTP routes**, `EntityKind::Endpoint`, `Relation::ServedBy`, schema v5, `py-framework` (FastAPI + Flask) | ✅ Complete (2026-08-03) — 1094 tests; **measured first**: `nerve impact` on a live `GET /users/{user_id}` handler and on a genuinely dead function printed **byte-identical** answers, and `nerve search users` found none of three `/users` routes — both closed, and the dead case asserted to stay dead so a fix that made everything look reachable cannot pass; the endpoint is the assertion's **source** because impact is a reverse closure, and `SERVED_BY` is **passive on purpose** — `DISPATCHES_TO`/`INVOKES` would assert a runtime invocation from a static registration; `SERVED_BY` joins `DEFAULT_RELATIONS` and the `COVERS` objection is answered rather than dodged — a coverage run is a report *about* the code that goes *stale*, an endpoint is a declaration *in* it that is *withdrawn*; **the regression test Slice 9b never got is committed here** — 9b shipped this defect one slot narrower, it was caught by hand, and no test could see it because all 1058 build fresh indexes; a probe ignoring the new `framework_version` slot fails it with *"files_re_extracted was 0"*; **first emitter of `FRAMEWORK_RULE`**, declared since Slice 1; the receiver is traced through two binding lookups rather than matched by spelling, because `.get` and `.route` are ordinary method names — `negative.py` holds five such decorators and a probe trusting the name emits **four forbidden endpoints**; **no prefix composition**, since a confidently wrong URL is worse than a partial one; **two corrections the fixture forced against the first draft** — a `decorator-form` tally member removed (making it fire needed a special case whose only purpose was feeding the counter), and a `methods=` case moved off FastAPI, which has no `route` decorator at all; **a reviewing agent found the lambda test passed vacuously** — the walker read only `decorated_definition`, so the applied-decorator form was never seen and the tally member had no producer; third vacuity trap caught on this project; FastAPI **12/0/0**, Flask **7/0/0**, never summed; unsupported reported repository-wide because an `app-not-local` construct has no framework to attribute it to; T1 attacked with a hostile **decorator factory** plus module top level, `setup.py` and `conftest.py`: **0 markers, 8 entities, 2 endpoints** — including the route above the factory |
| 10b | Framework rules — `ts-js-framework` (Express), its own precision table | ✅ Complete (2026-08-03) — 1116 tests; **Express 9/0/0**, its own table, never summed with FastAPI or Flask, and the ground truth written before the resolver **needed no correction**; a separate extractor id rather than a branch, and **zero `py-framework` observations in a repository with no Python** — the third restatement of the 5d-i invariant; Express has no decorator to key on, so the receiver is traced through the same two binding lookups — `negative.ts` holds a cache, a class named `Router`, an object literal and a `Map` all called with a string first argument, and the probe that skips the trace emits **six** forbidden endpoints; three refusals each with a test: **`app.all` is not a route** (expanding it would assert eight declarations for one statement), **a one-argument call is a lookup not a registration**, and **the last argument is the handler** so middleware is neither modelled nor guessed; `Views.handle` is declared a false negative rather than quietly resolved, because `py-framework` declines the same shape and resolving it on one language only would make `SERVED_BY` mean different things per language; **the upgrade trap's third instance** — 10a wrote `''` for TS/JS and 10b makes that wrong, so a committed simulation asserts re-extraction; **both language upgrade paths now have a test, and before this session neither did** |
| 11a | **Trace ingestion** — `nerve trace import`, `Relation::TestObservedCall`, `TEST_CALL_TRACE`, three-valued repository binding | ✅ Complete (2026-08-03) — 1168 tests; **Nerve runs no tests**, and the brief's offer to scope a subprocess exception was declined on the evidence: `no_subprocess.rs`'s own module doc names *"no test runners"*, and coverage (6a) and `gitinfo.rs` had both already chosen ingestion over execution — `no_subprocess.rs` and `no_network.rs` are **byte-untouched** across the whole row; **three corrections to the committed Slice 11 plan, each decided by existing code and verified on the database** — endpoints are `(caller, callee)` never `(test, callee)` (the graph holds `parse → tokenize`, where the plan would have asserted `test_basic → tokenize`, a call the test never made), `Directness::Resolved` because `Direct` overclaims and `Inferred` is coverage's value, and **no `TraceRun` entity so no schema change and no migration**; the implementation agent found a defect in my §2.1 claim and it was verified on the bytes — `idx_observation_identity` has seven columns and **no `environment`**, so two tests reaching one callee from one line are **one row**, and its union fix into `environment.runs[]` was right; `TEST_OBSERVED_CALL` is deliberately **outside** `DEFAULT_RELATIONS` (§8) with a second, security justification found during review: **T9's written control, "coverage may only produce `COVERS` — never a call edge", does not transfer**, because a trace legitimately produces one; my integration test was wrong **three times**, all over-assertion, all dropped rather than contorted — demanding every hostile artifact write zero observations (a bad *record* is refused while the import continues), demanding no artifact string reach output (the user pointed Nerve at the file), and demanding a byte-identical database after refusal (an `extractor_run` row recording the refusal is legitimate); **the implementation agent was killed by a mid-response connection close**, the fourth infrastructure kill on this project, and 2855 surviving lines were reviewed, compiled and green before the missing integration test was written by the orchestrator |
| 11a-i | Corrective — the hostile fixtures were not attacking anything | ✅ Complete (2026-08-03) — 1169 tests; **the token-expansion mechanism `fixtures/trace-hostile/README.md` documents did not exist** — `grep` for `__PAD_ARTIFACT__`, `__PAD_RECORD__`, `__PAD_STRING__` or `__INVALID_UTF8__` across `crates/` returned nothing, so `__PAD_STRING__` reached the parser as fourteen ASCII bytes and `__INVALID_UTF8__` as valid UTF-8; **four attacks tested nothing** while their fixture rows named the bound each was supposed to exercise — `oversized-string` and `malformed-utf8` refused nothing and wrote two observations each, `oversized-file` produced `malformed-json`, `oversized-record` produced `record-unknown-key` from its own padding key; the parser was never wrong (all fourteen forms in `trace::form::ALL` have unit tests and always passed) — **what was untested was the end-to-end path, while reading as though it were tested**, the fourth vacuity trap on this project after 8b's two T7 false positives and 10a's lambda test; **`run-id-conflict` was a real defect and the scope was the error** — detection compared only runs stored on the call site about to be restated, and the artifact replays its id on a *different* edge, so nothing stored ever saw it; the harm had been misplaced, since it is not overwriting but that **`run_id` stops naming one run**, and it is now repository-wide and counted once per artifact; **the aggregate threshold is why a green suite hid all of it** — *≥6 distinct forms across the whole set* was satisfied by the nine working attacks alone, so it is now **per artifact and bidirectional**, with the four inert artifacts required to produce **no** refusal because refusing a legal `run_id` for containing `NEAR(` would invent a rule the format does not have; `stage_hostile` refuses to stage an artifact still holding an unexpanded token, matching on prefixes so an unknown token also trips it; the new overwrite test's third assertion was wrong first — banning `9999` anywhere, when the replay's own edge legitimately carries it — so the assertion is now the **boundary** rather than the number |
| 11b | Reference Python tracer — `tracers/python/nerve_trace/`, `sys.monitoring` + `sys.settrace`, real pytest end to end | ✅ Complete (2026-08-03) — **1179 Rust tests + 115 Python tests**, the Python suite green on **three interpreters** (3.9.6, 3.12.13, 3.14.6) so the `settrace` fallback is exercised on the interpreter that actually needs it rather than only on the one that does not; nine modules, **pure standard library, zero dependencies**, `Cargo.lock` unchanged at 101; a **pytest plugin** because `sys.monitoring` reports code objects and cannot know which test is running — per-test attribution needs the framework's cooperation, which is why the contract carries `test_framework` — and **inert without its flag**, since a plugin that traced by being installed would change behaviour without being asked; **no Rust source may name the tracer** (new test, probe-verified) because product code that knows its name is one step from knowing how to launch it; the attribution rule is `frame.f_back` **and only when it equals the top of the tracer's stack**, and the second clause is the point — taking top-of-stack unchecked means that after any missed event the edge is recorded against *some* frame with no sign anything was wrong; from a **real pytest run**, `test_basic → parse → tokenize` lands as `src/parse.py:10 → src/lex.py:4` and **no test file is ever the caller of anything in `src/lex.py`**, matching the hand-written `bound.jsonl` line for line, which is a genuine cross-check between a human's reading and the tracer's; **three implementation pushbacks verified and accepted** — `completion_state: "interrupted"` does not exist (the brief was wrong; `CompletionState::ALL` is `[Crashed, Partial, Complete]`), a native frame *interposed between* two Python frames is undetectable without per-`CALL` monitoring whose cost is why PEP 669 has per-event opt-in (documented and declared, not papered over), and the header **omits three limitations on purpose** because overstating blindness misleads as much as understating it; **one pushback accepted only halfway** — `binding: bound` was reported *structurally unreachable* and was merely *unachieved*, so the orchestrator made the script `git init` the traced copy, index it, and import the **raw** artifact: measured **`bound`, 11 records, 11 accepted, 0 refused, 7 edges, 21 rows, exit 0**; that also closed a gap nobody had named — **nothing parsed the raw artifact**, since both the diff and the Rust test read the same *canonicalised* copy, which is the exact shape of the defect 11a-i was opened for; and canonicalising `git_commit` taught it once more — a `__GIT_COMMIT__` token **failed five of six tests**, because that is the one canonicalised field whose *shape* the reader validates (40 lowercase hex), so the sentinel is forty zeros, asserted exactly |
| 12a | Git **object access** — loose objects, `.idx` v2, pack entries, `OFS_DELTA`/`REF_DELTA`, alternates, shallow/partial detection | ✅ Complete (2026-08-03) — **1321 tests** (1179 → 1321); a reader and nothing more: zero entities, zero rows, `SCHEMA_VERSION` unchanged at 5, no migration, and `crates/nerve-store/` **byte-untouched**; the dependency **measured at +5, not the estimated +3** — `crc32fast` arrives for a gzip CRC Nerve never reads and `simd-adler32` because `flate2`'s `miniz_oxide` feature turns on `miniz_oxide/simd`, which is not a `miniz_oxide` default, and independent verification found **zero `.c`/`.h`/`.cc` files across all five** with `crc32fast`'s build script doing nothing but reading `rustc --version`; the security substance is that **`.git` object data becomes untrusted input** where until now Nerve read only two plain-text ref files, and the bound that matters is that the inflate is capped **as it streams** — orchestrator probe: turning the cap into a post-hoc check makes peak allocation **805 MB against a 64 MiB bound**, caught by a tracking global allocator measuring peak heap growth in its own test binary, which is the property rather than a proxy for it, and the bomb is generated from the bound per 11a-i; **two Rust footguns found and verified** — `flate2 1.1.9`'s `read::ZlibDecoder` returns `Ok(0)` rather than an error when input ends mid-deflate, so a truncated stream inflates to a silent empty prefix of a real object (worse than an unbounded read), and `checked_shl(7)` **does not detect overflow** because it validates the shift *amount*, so the first `OFS_DELTA` varint would have turned an absurd offset into a plausible base offset; **three corrections to the orchestrator's own plan, each proved against the code** — `selector_shape` is a *selector* guard that splits a `kind:body` qualifier and is the wrong tool for a filesystem path (T2 already names `canonical_child` as authoritative), the plan's "repository root check" cannot be passed in because the signature takes no root and in the worktree case `git_dir` is itself outside the repository, and **the worktree case is served by `commondir` rather than by alternates** — a linked worktree has no `objects/` at all, so without it the store opens and finds nothing, reading as *"this repository has no history"*, the exact failure the analysis warned about; SHA-256, `.idx` v1, `.midx` and commit-graph each refused **with a stated reason**, and "no magic" is distinguished from "version 1" so a corrupted v2 index is not reported as a 2007 format; fixtures include a real **2275-byte packfile**, 19 objects, 5 genuine deltas, generator *fails* rather than emit a delta-free pack, committed with a fixed synthetic identity and fixed dates so no developer identity is in the binary |
| 12b | Git **history ingestion** — schema v6, commit + change + availability tables, exact-content rename hypotheses, `nerve history sync` / `log` / `file` | ✅ Complete (2026-08-04) — **1402 tests** (1321 → 1402) across four commits: fixtures `24da6ef`, storage `4b0d926`, engine `848af72`, CLI here; the **storage strategy was measured, not assumed** — per-commit snapshots cost 22,958 path-rows against 762 change-rows on Nerve and 682,940 against 3,858 on a 1,214-commit repository, and the ratio *grows with depth* because snapshot is `O(commits × tree)` and delta is `O(churn)`, so one repository would have understated it; **"commit entities" declined** against the roadmap row's own wording, because Nerve has ruled twice and stated the rule both times — `CoverageRun` is an entity for having to be an *endpoint*, `TraceRun` is not for being *provenance* — and a commit is provenance for a change fact whose subject is a **path**, which is not a current entity, so **no entity kind, relation or source type is added** and `entity_fts`, `symbols_total`, selector resolution, `ui_vocabulary.rs` and zero frontend lines are touched; **the invariant is that a shallow boundary is never diffed against the empty tree** — doing so reports every file in the boundary tree as newly *added*, the claim stated as data, and the orchestrator's probe fails with *"2 of the 2 paths in the boundary tree were reported as newly added"* while needing **two** mutations rather than one, because reclassifying the boundary alone leaves the test green when the parent object is genuinely absent; `may_claim_history_begins_here()` is true for `root` and nothing else and is the **only** copy of the rule — the CLI calls it and the JSON exports it so neither re-derives it; **five reasons a parent is invisible, not four** — adversarial review found `read_pointer_file` returns `None` for absent, over-bound and unreadable alike, so an unreadable `.git/shallow` reads as *not shallow* and every boundary would have been called a fault, the inverse of the error being guarded; a **sixth** reason the brief never named is `commit_budget`, which is Nerve's own doing and never a property of the repository; zero changes has **four** meanings and `changes_enumerated` is which, so a merge's silence never reads as emptiness; renames are **hypotheses** with `ambiguity` and no score, every pairing of an ambiguous exact-content match recorded and none promoted; **two pre-existing defects fixed** — `gitinfo::head_commit` did not follow `commondir`, so **indexing a linked worktree recorded no `repository_state.git_commit`**, and `canonical_child` cannot guard a historical path because it canonicalizes, which would have refused every `deleted` change and made rename hypotheses *structurally always empty* **while counting each refusal as a path-safety success**; **fourteen corrections to this slice's own plan across four reviews**, including that `parent_completeness` and `changes_enumerated` are *not* immutable so an unshallowed boundary would keep false availability forever, that §8.5's "stop at the first recorded commit" made that repair unobservable (probed at zero re-records), and that **criterion 5a was unsatisfiable** — `history-hostile` puts its four hostile names in the same tree as the subtree `..` and `parse_tree` refuses a whole tree, so the criterion would have been met by a counter that could never leave zero, the 11a-i shape caught by the implementer; **fifth vacuity trap, first caught by an author** — an ordering test passed with its `commit_oid` tiebreak deleted because `idx_git_commit_time` returns ties in reverse rowid order; `INSERT OR IGNORE` **raises** on a foreign-key violation but suppresses a duplicate PK (verified against SQLite), so the FK test would have passed either way and the duplicate-row test is the load-bearing one; measured on Nerve's own 95 commits: **898 of 1446 subtrees skipped**, warm re-sync **14 ms** against 257–314 ms cold; **the CLI agent was killed mid-slice by an org monthly spend limit**, the third such kill on this project, and its 1029 surviving lines were inspected, kept and finished by the orchestrator |
| 12c | Git **historical questions** — first/last seen with boundary qualification, similarity rename hypotheses, change frequency, labelled co-change, state-to-state diff, API + MCP + UI | ✅ **ROW 12 COMPLETE.** **12c-ii closes it** — schema **v7**, the `nerve-line-multiset` matcher, and every surface. Report: `docs/reports/slice-12c-ii-report.md`. Four commits: plan correction `2517736`, storage `d20abfd`, matcher `aedf952`, surfaces `8f91cf7`. **1492 → 1532 tests**, frontend **29 → 34**, acceptance **43 → 50**, `Cargo.lock` unchanged at 106. **The committed plan was refuted before code was written**: §3 claimed similarity renames needed no schema change, and `git_rename_hypothesis.blob_oid TEXT NOT NULL` disproves it by inspection — one blob column exists *because* an exact-content hypothesis means both paths name the same blob, so a similarity pair, which names two different objects, has no honest value to put there. `evidence` + `ambiguity` also cannot record the matcher, its version, the measurement, the threshold or candidate-set completeness, and a hypothesis a reader cannot reproduce is not inspectable evidence. v7 rebuilds the table on `from_blob_oid`/`to_blob_oid`, stores the measurement as **two integers rather than a float**, and adds a `CHECK` that makes blending exact and similarity evidence a **`ConstraintViolation` rather than a code review** — proved by probe (extended code 275). `git_rename_analysis` is **per commit** because the decisive case has no row to carry a flag: a bound-refused commit records *no* hypothesis, and an absence would have to be interpreted again. Exact-content renames get **no** analysis row, and that is a claim: the exact matcher reads no blob content, so it is complete exactly when `changes_enumerated` says the diff was. `git_commit.summary_truncation` is a **three-value vocabulary, not a boolean**, because a v6 row cannot be backfilled and a 600-byte subject stores at exactly 512, so `length = 512 ⟹ truncated` would manufacture a false positive on the one boundary case that matters. **The threshold is an output of measurement, not a preference**: `c5`, two unrelated documents sharing a sixteen-line licence header, measures **16/20 = 4/5 exactly**, so any threshold ≤ 0.80 admits it and FP = 0 becomes unreachable; 7/8 rejects it (`128 < 140`) and still admits 18/20 (`144 ≥ 140`). **similar-content: 13 candidate pairs · 6 admitted · 6 true positives · 0 false positives · 4 correctly rejected · 1 false negative · 2 unmeasurable · recall 6/7.** **exact-content: 1 pair · 1 admitted · 0 false positives.** Two tables, never summed. The false negative (`c2`, a genuine move with twelve of twenty lines rewritten) is **asserted to be reported**, which is what stops a later change lowering the threshold to flatter the number. Ground truth is **hand-written before the matcher existed**, keyed by commit summary rather than object id so it cannot be a derived artifact, and the generator copies it aside and back so the script that builds the corpus cannot overwrite the file that grades it; the orchestrator **recomputed every measurement independently from the raw git objects** and it matched case for case. **The narrow-width QA gap open since row 7a-ii (2026-08-02) is closed, and the obvious approach was a trap**: `--window-size=380` writes a 380px PNG while `window.innerWidth` reports **500**, because Chrome clamps a headless window to a platform minimum — the page lays out at 500 and the screenshot is merely cropped, which is the *same* failure as the extension's `resize_window`. The mechanism that works is CDP `Emulation.setDeviceMetricsOverride` over Node's built-in `WebSocket` (no dependency, no network), committed as `scripts/viewport_qa.mjs`, and it **asserts** the viewport it got. Measured at 1600px and 380px over five routes: no page-level overflow, zero console messages, zero exceptions, zero 4xx, database byte-identical across the session. **It found a real defect this slice introduced**: `.chip` is `white-space: nowrap`, right for `similar_content` and wrong for a 49-character sentence, so `div.row--wrap` measured `scrollWidth 379` against `clientWidth 304`; fixed with a `chip--prose` modifier so tokens keep not wrapping. **A latent FK bug from Pass A was caught by Pass B**: `git_rename_analysis` carries the same `git_commit` foreign key with no `ON DELETE CASCADE` while the history repair deleted only two of three dependent tables, so the first analysed commit would have failed `history sync`. **Two subagents were killed by session limits** (Pass B after delivering its report, Pass C on its final probe); the orchestrator verified no live mutation remained, which a green suite proves since every probe is built to fail a named test. **Not covered:** systematic keyboard navigation and the corrupt-history UI, both carried into the functional UI parity phase.<br><br>**12c-i complete** — **12c-i-b** (`ff76b8c`) adds the CLI: `history diff`/`frequency`/`cochange`/`availability` plus the first/last block on `history file`, **1438 tests** (+9), acceptance 43/43, no store function and no new exit code. Its subject is a **refusal**: `git_change` is path-keyed and `path_role()` gives the four symbol kinds `PathRole::None`, so a symbol selector is refused *as a refusal* and **the containing path is never extracted** — guessing it is the failure, not a recovery. Detection comes from the selector grammar rather than an invented rule, and the residual is stated: Git permits `#` and `:` in tree paths, so a path spelled that way is refused, which is the trade `selector_shape` already makes. Only `may_claim_created` licenses the word "created", read off the response with `unreachable!()` in the mirror arm; verified by the orchestrator on the shipped binary against the shallow fixture with **zero** occurrences of "first ever"/"was created"/"project began". `history diff` renders five outcomes distinctly and **every diff-shaped JSON key is present on every outcome, `null` on the four refusals**, so `commits: null` (no range computed) and `commits: []` (range computed, empty) are distinguishable — a refusal reading as an empty diff is now detectable rather than a judgement call. Three brief corrections: "no new wording functions in the CLI" was **unsatisfiable** because `FirstObservedKind`, `HistoryFreshness` and `EarlierHistoryUnavailable` have no `note()` (**fold that hoist into 12c-iii** before three more surfaces copy the prose); `WalkBudgetExhausted` was unreachable with only `--limit`, so `--max-walk`/`--max-changes` were added because a bound that cannot be exercised cannot be tested; and §14.3's `PresentBeforeVisibleHistory`-on-`history-shallow` is unreachable for a **second** reason — the checkout holds neither historical path. **The 12c-i-a wording guard fired on its first real test**, catching a `WalkRefused` arm that contained `WalkTermination::Refused.note()` verbatim. **One 12b write-path defect found and deliberately not fixed here** (this is a read surface): `delete_commits_with_unavailable_parents` (`history.rs:490`) deletes unconditionally each sync assuming the re-walk re-reaches those commits, which fails once HEAD moves — and the defect is the **asymmetry**, since a commit with *available* parents that becomes unreachable stays recorded. Recorded for a corrective slice. **12c-iii-a complete** (`9179388`) — **seven history endpoints**, `ROUTES` 11 → 18, **1460 tests** (+22), acceptance 43/43. The hoist landed first because every surface added before it copies prose: `FirstObservedKind::note`, `created_claim_note` and `HistoryFreshness::note` to `nerve-core`, `EarlierHistoryUnavailable::note` to `nerve-store`, CLI −238 lines. **Part A named three notes; there were four.** The wording guard grew a **per-note owner file** across four crates and asserts *both* owners are used, so collapsing every note into one file fails rather than quietly going vacuous. **The route-parity probe found a real hole**: nothing kept `router.rs`'s dispatch match and `ROUTES` in step, and because `guard.check` runs at `:112` *before* `route()` at `:146`, an entry naming no arm still answered 401 and passed `no_api_route_answers_without_a_token` — while the **reverse direction was unchecked entirely**, so a route in the match but not in `ROUTES` would never be token-tested at all. Now a set comparison in both directions plus a live-server drive, with the anti-vacuity floor deliberately low so *removal* is caught by the set assertion rather than by a count. **Two brief constraints conflicted and the resolution is a move, not a copy**: `history_path_refusal` was private to a binary crate and unreachable from the server, so it moved to `nerve-store/src/select.rs` beside `selector_shape` — without it, `?path=src/app.ts%23parse` would have answered with the file's dates. **One residual pinned**: `Target::parse` drops everything after a raw `#` (`request.rs:58`, correct HTTP fragment semantics), which means a refusal test written with a raw `#` passes against a handler with **no refusal at all** — every case uses `%23`, and handoff Entry 7 makes it a UI requirement. **12c-iii-b complete** (`731bebd`) — **one** `nerve_history` MCP tool, `TOOL_NAMES` 5 → 6, **1483 tests** (+23), acceptance 43/43. Not seven tools: the admission test is this project's own (`slice-08-mcp.md:50-53`, a tool earns its place by a *materially different input/output contract*), and the seven questions fail it **structurally** — every one returns the same availability block because §9 requires that block assembled in exactly one place, so seven tools would have differed only in which subject is named. The mode-switch trade is paid down by a closed `question` enum, per-question `if`/`then` requirements in the schema, `result_kind` naming the shape inside the answer, and — the part that matters — an argument table enforced in **both** directions, so `question=frequency&path=…` is refused rather than silently ignoring `path`. **Three findings.** The orchestrator's brief cited a plan §6.5 that **does not exist** (the plan's §6 is similarity renames; that was the user brief's numbering), and the implementer fell back to the recorded Slice 8 rule rather than inventing one to fit the citation. **`tool::validate_selector` must not screen a history path** — Git accepted `../escape.txt` and `sub/../../escape.txt` as real tree entries, so screening would refuse real questions about real recorded paths *while counting each refusal as path-safety coverage*, the §4.4 failure exactly; nothing is risked because the argument becomes a bound SQL parameter and `nerve-store` opens no path. And **the first T7 extension was vacuous** — the helper's own anti-vacuity assertion caught it, because hostile *paths* never reach the history tables (`safe_tree_name` refuses them at ingest), so the reachable hostile content at this surface is the commit **summary**. One limitation stated rather than papered over: the 128 KiB ceiling is unreachable over the wire on any committed fixture, so it is proved where it is applied with a synthetic 512 KB answer. **12c-iv complete** (`397ea3e`) — four history views, `history.ts` with its own tests, routes wired, the embedded bundle rebuilt, **1492 Rust tests** and **29 frontend tests** (was 15), acceptance 43/43. **The eight unmirrored vocabularies are glossed and their guards land in the same commit**: `ui_vocabulary.rs` pinned seven while eight were missing, and it *could not fail* for them because it did not know they existed — the exact drift 5d-iii was a corrective slice for, where it found 120 sites rendering fallback text. Orchestrator probe: removing one gloss fails `every_change_kind_is_glossed` by name. The row's wording rule holds in the view layer — `HistoryPath.tsx:102` reads `may_claim_created` off the response rather than deriving it from `kind`, renders `may_claim_created_note` verbatim, and the only occurrence of "first ever" in the interface source is inside a test asserting it never appears. **A session limit killed the implementation agent — the sixth infrastructure kill on this project — at a clean boundary** (frontend complete, bundle rebuilt, Rust gate not started), and the recorded fallback applied: inspect, preserve, verify in the orchestrator. **⚠️ DEVIATION: browser QA was NOT performed** and no visual verification is claimed; that is the one open gap in this slice and the next session's first action. **Remaining: 12c-ii** similarity renames closes row 12 (order changed, `a67f55a`).<br><br>**12c-i-a** (`57c7086`) — **1429 tests** (1402 → 1428 implementer → 1429 after orchestrator corrections), acceptance **43/43**, schema still v6 with `schema.rs` byte-unchanged and `Cargo.lock` at 106. Six `FirstObservedKind` values, four `HistoryFreshness` verdicts, five derived queries, and **the wording hoist that was four functions plus a judgment** — `walk_termination_note`/`availability_note`/`enumeration_note`/`ambiguity_note` were inside the CLI binary where HTTP, MCP and the UI could not reach them, and `earlier_changes_may_exist` is worse for being the one *interpretation* every history surface must agree on; single-copy is now enforced by a byte-level source scan whose sentinels are **generated from the vocabulary** and which a planted NUL cannot blind. **The slice's own plan carried a defect and `git` settled it**: "may say created" was `Added` + `may_claim_history_begins_here()`, true only for a root commit, so a file added in commit #5 of a *complete* clone reported `EarliestVisibleChange` — *history above may be hidden* — beside two fields stating nothing was hidden, making creation unreachable for ~414 of ~420 files here. `fixtures/history-basic/inventory.json` carries Git's answers (seven paths, **one addition each**, `src/app/extra.rs` added at `522afd34` with parent `68cde314` present, not shallow), so Nerve was contradicting the one oracle that is authoritative rather than comparable. The rule is now *an addition · nothing hidden above this path · **exactly one recorded addition***, which keeps the reason the parentless rule looked right — `first` is ordered by `committer_time` and a rebase reorders it freely — because a path created, deleted and re-created records two additions; a two-addition path therefore hedges **although nothing is hidden**, that refusal being about *ordering* rather than availability. `ParentCompleteness::Root` returns "nothing hidden" immediately after a first fix that short-circuited on `ingest.shallow` was caught by an **existing** control assertion (a shallow clone can hold a genuine root), which exposed a scope error in the orchestrator's own invariant: the reason is **path-level** and the boolean is **repository-level**, so they may only be required to agree when the anchor has available parents. `EarlierHistoryUnavailable` gains `WalkRefused` because `Refused` had named no reason beside a `true` boolean — two derivations of one question disagreeing, inside the slice written to remove that. Four implementer findings against the plan, one of which is that **§7's "the tiebreak is load-bearing" is false on this schema and was measured**: `EXPLAIN QUERY PLAN` shows `idx_git_change_path` with no temp b-tree for `GROUP BY`, deleting `path ASC` left every ordering assertion passing, so both clauses are kept and the guard is a **source-level** test labelled as one. (superseded by the 12c-i-b entry above)<br><br>Plan: `352c824`, corrected `2b558af` and again by the implementation, `docs/plans/slice-12c-historical-questions.md`. **Four sub-slices**: 12c-i queries + wording hoist + CLI, 12c-ii similarity renames, 12c-iii API + MCP, 12c-iv UI + glosses. **No schema change, and it was checked rather than intended** — `idx_git_change_path` (`schema.rs:367`) serves the frequency aggregate, the PK `(repo_id, commit_oid, path)` serves the co-change self-join, and `git_rename_hypothesis.evidence` already carries the comment *"similar_content added in 12c"*. Settled: **relationship appearance/disappearance stays refused** (needs the historical graph; over the current graph it attributes today's edges to yesterday's commit), and **symbol-level history stays refused** — the enforcement is that a symbol-shaped selector is refused *as a refusal* rather than answered with its file's dates. Two findings from reading the code that the plan was corrected for: the wording hoist is **four functions plus a judgment** (`walk_termination_note`/`availability_note`/`enumeration_note`/`ambiguity_note` and `earlier_changes_may_exist`, all inside the CLI binary at `main.rs:2151-2260`), and `FirstObservedKind` needs **six** values, not five — `main.rs:361` documents that `history sync` requires only `init`, so with no entity table Nerve cannot tell "exists now, never changed" from "does not exist now" and either answer is a claim without evidence |
| 13 | Cross-repository contracts | ✅ **ROW 13 COMPLETE** (2026-08-10). Six sub-slices: **13a-i** `2509511` schema **v8** (`repo_registry`, `contract_link`), **13a-ii** `4965a76` registry CLI + the second-repository trust boundary + **T12**, **13b** `b2afaba` C1 npm local/workspace + C3 Python path, **13c** `f336d5c` C2 npm export resolution, **13d** `672eaa1` HTTP + MCP + UI, **13d-ii** `09d050d` `nerve repo links`. **1657 tests, acceptance 75/75, `Cargo.lock` unchanged at 106.** **The plan was corrected before any code was written** (`2b91a25`), and the correction is the row: **C2 cannot be a local assertion.** `assertion.target_entity_id` is `NOT NULL REFERENCES entity(entity_id)` (`schema.rs:97`, immutable since v1) with `foreign_keys=ON`, and a cross-repository target has no row in the local `entity` table — so the insert *cannot succeed*, and the only ways to force it (a proxy entity for something A never indexed, or dropping the FK) are refused. Every link lives in `contract_link` with a foreign-keyed **source** and an unkeyed **target snapshot**; `neither_path_nor_impact_traverses_a_contract_link` asserts the boundary negatively with two anti-vacuity floors, so "the answers matched" cannot pass as two empty answers. The new global `EvidenceSourceType` was **withdrawn** with it — `observation.assertion_id` is `NOT NULL` (`schema.rs:115`), so no observation could ever carry it; `ALL` is still **12**. **Three precision tables, never summed**, each FP = 0 with recall *reported*: C1 5 TP · 0 FP · 1 FN · 0.833 · C3 5 TP · 0 FP · 1 FN · 0.833 · C2 8 TP · 0 FP · 1 FN · 0.889. Every FN is a **declined true link** — a form outside the enumerated syntax that really does name a registered neighbour — counted and published rather than bought back by widening the syntax. **13b argued its own recall down from 1.000 to 0.833** on that basis. **`contract_version_mismatch` has no producer and §9.3 was amended to require eleven of twelve**: `^1.2.0` against `1.2.3` is range satisfaction, not string inequality, so deciding it needs a semver resolver — a new dependency. Both versions are recorded; no verdict is derived. Requiring a state nothing can produce is the `generated_client_stale` mistake, which the same plan correction had already removed. **T12** covers the first read of a repository the user did not point Nerve at: seven controls, each with a test paired against the positive half that stops it passing vacuously, plus **one measured residual** — a read-only open of a WAL database makes SQLite create two coordination sidecars in the neighbour, so §7.4's "and nothing else" is not literally achievable that way, and `immutable=1` was refused because it ignores the WAL and would read a neighbour stale while reporting it current. **13d found a gap in itself** — HTTP/MCP/UI could answer "is this link still current?" and the CLI could not — and 13d-ii closed it, with acceptance asserting **cross-surface agreement** on `(freshness, note, is_current)` keyed by `link_id`, floored at two distinct real verdicts. Ten further plan corrections are recorded in the plan's own correction blocks. Superseded plan entry: 🔵 Plan accepted and committed (`307b029`), `docs/plans/slice-13-cross-repository-contracts.md`. Four sub-slices: 13a registry (schema v7), 13b npm + Python path dependencies, 13c npm export resolution, 13d surfaces. **The contract set was chosen on dependency evidence**: `serde_json` and `toml 0.9` are already workspace dependencies of `nerve-index`, so `package.json` `file:`/`workspace:` dependencies, `package.json` export resolution and `pyproject.toml` path dependencies cost **zero new packages** and have entities on both ends. OpenAPI **deferred with both costs named** — most published OpenAPI is YAML, which needs a dependency, and matching `{id}` against `{user_id}` is a normalisation Nerve would be inventing rather than a declaration it quotes. The architectural decision: a cross-repository link lives in the database of the repository the command is run from and the target is opened read-only, so **a link is directional and one-sided** — writing into both databases would mean indexing A writes into B, which Nerve has never done, and a global registry database puts a writable location outside every repository. **No new relation name**: C1/C3 have no entity on either end (12b's commits-are-not-entities argument again), and C2 reuses `REFERENCES` because relation names are endpoint-kind-agnostic, so the cross-repository-ness becomes the first new `EvidenceSourceType` since 5d-i |
| 14 | Human-confirmed memory | 🟡 **14a + 14b complete; 14c and 14d remain.** **14b-ii** (`bdc87fa`) — the ten-verb `nerve memory` family (`propose confirm supersede invalidate cite list show search events export`) over 14b-i's transactional store, deterministic versioned export, and **the existence-only acceptance loop deleted in the same commit that landed the command** — which is what §6 of the plan demanded and what `2dc3a7d` had to do for `history`. Acceptance **75 → 83**, tests **1713 → 1730**, `Cargo.lock` 106. **A 14b-i defect was found by building on it and fixed here**: `supersede_memory` reached the raw `insert_memory`, so a record born by supersession arrived with an **empty audit history** while an identical record born by `propose` arrived with a `proposed` event — `nerve memory events <successor>` printed nothing and the record could not say it had ever been written. §4's *"every mutating lifecycle operation appends a typed event"* carves out no exception for the operation that **creates** a record, so the successor's creating event is now appended in the same transaction; four anti-vacuity event-count floors across three files moved by exactly one. The implementer had recorded the hole in a test asserting the empty history rather than papering over it, which is why it was findable. **Two claims in this plan and in `CONTINUATION.md` were false and the repository settled it**: both said a CLI-surface test holds `nerve affected` and `nerve trace-tests` refused, and **there is no such test** — `affected` appears in no CLI test at all and `trace-tests` only in doc comments and the acceptance script, so those two refusals are held only by a script a developer may not run. 14b-ii built both halves for its own verb (`there_is_no_delete_verb` plus a `refused_verb` acceptance row proved non-vacuous by pointing it at `memory show`), and the carried defect is recorded. Export **omits three things on purpose** (`a466c25`): no `exported_at`, because §7.4e promised byte-identical twice and a timestamp cannot coexist with it; no derived state, because writing a query's answer into a file that outlives the query undoes the stored-versus-derived split; and no absolute path. Two mutation probes, each failing a named test and reverted byte-identical. **Import deferred explicitly** — export is the supported backup mechanism, and a half-safe importer is how a human's notes get overwritten by a file. Superseded 14b-i entry follows: **14a + 14b-i complete; 14b-ii, 14c, 14d remain.** **14b-i** (`dd55152`) — schema **v10**, closing `memory.scope` and `memory_event.operation` in SQL, plus the transactional lifecycle writes (`propose_memory`, `confirm_memory`, `invalidate_memory`, `cite_memory`). **1713 tests**, `Cargo.lock` 106, `EvidenceSourceType::ALL` still 12. Report: `docs/reports/slice-14b-i-report.md`. **The `scope` domain was decided before any code** (`169f37a`) and the argument is deliberately *not* the one that closed `status`: `status` was closed in SQL because a derived view could be stored, which does not transfer. What applies is that `scope` sits in **both** grouping keys — `(subject, scope)` decides `multiple_active`, `(subject, scope, claim_key)` decides `conflicted` — so a **typo silently suppresses a conflict report**, a false negative on the one contradiction claim the feature makes that no test can catch because both spellings are legal. The values are on an axis the neighbouring columns do not occupy, and the first draft duplicated both: not the subject's kind (`subject_kind_snapshot` holds that, and 14a's test values `"file"`/`"repository"` **are** that redundancy), not the question answered (`claim_key` holds that) — but **which facet of the subject the claim is about**, forced by the plan's own `owner` example, where *"the payments team owns this code"* and *"platform owns its deployment"* share a subject and a claim key and are **not** a contradiction. Four values (`implementation`/`interface`/`operations`/`process`), each admitted only because a real note in this repository needs it; `ownership` refused as a value because *owner* is a `claim_key` and one concept may not sit on two axes. **The migration is the slice, and v7's rebuild is not the precedent it looks like**: v7 rebuilt `git_rename_hypothesis`, which nothing referenced, whereas `memory` is referenced by two child tables **and by itself**. Three facts were measured rather than assumed — `PRAGMA foreign_keys` is a no-op inside a transaction (it still reads 1 after being set OFF inside BEGIN) and every migration step runs in one; `DROP TABLE` with FKs on performs an implicit `DELETE FROM` that orphans every child row; and **`defer_foreign_keys=ON` does not rescue it**, because the implicit delete increments SQLite's deferred-violation counter and the rename never decrements it, so the commit fails with `FOREIGN KEY constraint failed` **while `PRAGMA foreign_key_check` reports zero rows** — consistent database, refused commit, failing outside the step that caused it where the obvious diagnostic cannot see it. The step therefore keeps enforcement **immediate** and orders statements so each is consistent at its own conclusion (TEMP staging · children emptied then `memory` **unqualified**, because only a whole-table delete satisfies the self-reference · drop · **re-create under their own names** · re-insert), with **no `ALTER TABLE … RENAME` anywhere** — since 3.25 a rename rewrites references in *other* tables, so renaming `memory` aside would silently repoint `memory_citation`'s foreign key at the scratch table about to be dropped. **An out-of-domain row refuses the migration**, naming the offending values, rather than being dropped or defaulted: memory is the one thing re-indexing cannot rebuild. `fixtures/ts-basic/golden.json` moves by **exactly one line** (`schema_version` 9 → 10), which is the evidence the rebuild perturbs nothing in the graph. Two refusals the implementation added: a **proposal may not name a predecessor** (the unique index would give it its one successor, so no later call could retire it — unrecoverable), and **invalidation is refused from `superseded`** in both directions, which is what keeps the two statuses distinguishable. Three mutation probes, each failing a named test and reverted byte-identical. Superseded 14a entry follows: **14a complete** (`2190643`) — schema **v9**: `memory`, `memory_citation`, `memory_event`. **1700 tests**, acceptance 75/75, `Cargo.lock` 106, `EvidenceSourceType::ALL` still 12. **The plan was corrected before any code** (`d9120c8`), and the correction decides the schema: **`memory.subject_entity_id` must not be a foreign key into `entity`.** Entity rows are deleted routinely — `prune_orphans` (`prune.rs:376`, `:440`), pinned as required behaviour by `deleting_a_file_removes_its_entities_assertions_and_observations` (`incremental.rs:290`) — so with `foreign_keys=ON` the FK either **blocks re-indexing a file a human wrote a note about** or **cascade-destroys the note on a routine re-index**. Memory stores subject snapshots and resolves at query time (`resolved` · `resolved_through_identity_link` · `missing` · `ambiguous` · `repository_state_unavailable`); `memory_citation` has the identical problem and the identical fix. Proved end to end rather than argued: `a_note_survives_the_re_index_that_prunes_its_subject` writes a note, deletes the file, runs the **real** `index_repository`, and reads the note back intact with resolution `missing` — a refused delete fails at the entity count, a cascade fails by name. The machine tables (`assertion`, `observation`, `occurrence`, `assertion_state`) are byte-identical across every memory operation by **two** mechanisms — a full canonical serialisation *and* a BLAKE3 digest over an indexer-built database — with a control test proving the comparison is not blind. **Two notes on one subject are not a contradiction**: `conflicted` requires a shared `claim_key`, keyless records are `multiple_active`, asserted **negatively**, because the rule as drafted would have called every second note about a file a conflict — `ADR_DESCRIBES_COMPONENT`'s refusal in a new place. **Orchestrator correction: the four stored statuses are enumerated in a `CHECK`.** 14a left `status` unconstrained in SQL on the ground that this schema closes vocabularies in Rust (which `V4`'s comment does say), but **v7 already contradicts that as a blanket rule** — `git_rename_hypothesis` enumerates its two evidence values precisely because an invariant depended on the column's *domain*. Here §7.4's invariant is that a derived view is never stored, and with the vocabulary closed only in Rust raw SQL can write `potentially_stale`, failing on the next read — loudly, but **after the row is on disk**, which is a repair job rather than a refusal. Cost stated: a fifth *stored* status needs a table rebuild, and that is the intended price. Probe: deleting the `CHECK` fails the new test by name. Supersession stores **one** direction with a unique index, and the status flip and event append are one transaction — asserted by a refused supersession leaving neither half applied. V1–V8 byte-identical. **Remaining: 14b-ii** the `nerve memory` command family + citations + export (14b-i landed the store layer it builds on), **14c** read-only HTTP + MCP + T7 + T13, **14d** UI + the semantic acceptance checks that must replace `final_acceptance.sh`'s existence-only `memory` row (§6's own recorded defect — the loop currently prints NOT BUILT, and the moment `nerve memory` exists it would print a `PASS` that checks nothing). Eight further under-specified points are recorded in the plan. Superseded plan entry: 🔵 Plan accepted and committed (`eb68830`), `docs/plans/slice-14-human-confirmed-memory.md`. **"Agents cannot self-confirm" cannot be an identity check** — Nerve has no accounts, no network and no identity provider, so at a local CLI an agent invoking `nerve memory confirm` is byte-indistinguishable from a human. The control is a **surface boundary**, which unlike an identity claim is testable: the confirm path is *absent* from `crates/nerve-server/src/mcp/` rather than gated inside it, asserted by a source scan — plus **T13**, recording that a human who hands an agent their shell has removed the boundary and Nerve cannot detect it. **Memory is not an `Assertion`**: an assertion is a relation between two entities, and `assertion_state` is defined as a pure function of machine observations, so a human sentence inside it is the silent-truth failure the brief forbids, reached through the schema rather than through a feature. Own tables, and the invariant is mutation-probe-able — `assertion`/`observation`/`occurrence`/`assertion_state` byte-identical across every memory operation. **The HTTP API stays read-only** (Slice 4a proves `query_only` on the bytes), so the UI displays memory and prints the exact command for confirmation. `nerve memory delete` is **refused, not deferred** — a delete verb is how "history preserved" stops being true |

## Slice 1 — delivered

- 4-crate Rust workspace (`nerve-core`, `nerve-store`, `nerve-index`, `nerve-cli`).
- SQLite schema v1 with the eight-concept evidence model + FTS5 symbol search.
- Tree-sitter extraction for `.ts`, `.tsx`, `.js`, `.jsx`, `.mjs`, `.cjs`.
- Entities: Repository, Directory, File, Module, Function, Method, Class, Interface, Unresolved.
- Relations: `CONTAINS`, `DEFINES`, `IMPORTS`, `EXPORTS`. Unresolved imports retained as first-class entities.
- `assertion_state` proven to be a pure rebuild from observations.
- CLI: `init`, `index`, `status`, `search` with `--json`.
- Security: secret deny-list, `.gitignore`/`.nerveignore`, path-traversal and symlink-escape guards, `0600` DB.

## Deferred out of Slice 1 (deliberate)

`CALLS`, `REFERENCES`, `EXTENDS`, `IMPLEMENTS` — deferred to Slice 2 so they ship with negative
fixtures and a measured precision number rather than as name matches. See master plan §3.3.

## Slice 2 scope split (2026-07-31)

Roadmap row 2 was one row covering a resolver, a measurement apparatus, and two query
surfaces. It is split into **2a** (resolution + relations + measured precision) and **2b**
(`nerve path`, `nerve why`). Rationale, pushback and acceptance criteria:
`docs/plans/slice-02-static-resolution.md`.

## Slice 2a — delivered

- Second extractor `ts-js-reference 1.0.0`, declaring `[AST_DIRECT, AST_RESOLVED]`.
- Lexical binding table with `Opaque` shadowing guard; transitive re-export closure.
- `CALLS`, `REFERENCES`, `EXTENDS`, `IMPLEMENTS` — resolved edges `AST_RESOLVED`, unnameable
  targets recorded as `Unresolved` entities with a closed-vocabulary reason.
- **Measured: FP=0, FN=0 across 24 resolved edges; 38.1% of call sites honestly unresolved.**
  Gate validity confirmed by orchestrator mutation probes against the implementation.
- Corrected a Slice 1 defect: resolved `IMPORTS`/re-export `EXPORTS` were mislabelled
  `AST_DIRECT`; now `AST_RESOLVED` (`ts-js-structural` → 1.1.0).
- Corrected an identity collision: unresolved ids now carry a `module`/`value` category.
- No schema change, no new dependencies. Report: `docs/reports/slice-02a-report.md`.

## Slice 2b — delivered

- `nerve path <from> <to>` — bounded-depth simple-path walk with relation/direction filters,
  an honest `truncated` flag, and exit `0` on "no path" (absence is not an error).
- `nerve why <from> [<to>]` — the full evidence packet per assertion: every observation with
  source type, directness, extractor id + version, `file:line`, details, and **computed
  freshness** (the file is re-hashed at query time).
- Selectors refuse ambiguity: multiple matches exit `10` listing candidates; nothing is guessed.
- Query-time file reads reuse the Slice 1 path-safety choke point. Verified by constructing
  symlink-escape attacks: both file-level and parent-directory escapes are `refused`, with zero
  content leakage. Queries are provably read-only.
- Traversal p95 ≤ 83.45 ms at depth 4 on 2 M assertions, against ADR-0001's 200 ms budget
  (measured under adverse machine load; see report for the attribution of one spurious failure).
- No schema change, no new dependencies. Report: `docs/reports/slice-02b-report.md`.

## Slice 3 — delivered

- **Deletion works.** Before this slice the pipeline only `INSERT OR IGNORE`d, so a removed
  file's entities and edges survived forever and the graph was wrong after any deletion.
- Change classification + **transitive** invalidation closure over `IMPORTS` (a barrel-file edit
  reaches importers that never name the edited file).
- **Equivalence property:** an incremental re-index is byte-identical to a full index of the same
  tree, verified at every step of a seeded 24-edit sequence across six edit kinds.
- `IdentityLink` populated for moves, proposed with evidence; a coincidental name match proposes
  nothing.
- Schema **v2** (additive: `module_facts` cache), with v1→v2 migration tests.
- `nerve index --full`; removals reported loudly.
- **Speed target missed:** 24.9% of a full index on a realistic corpus vs a < 20% target.
  Amplification 1.00. Cause is the O(repository) state-restatement pass forced by `state_id`
  being inside `occurrence_id` (ADR-0002) — hence Slice 3b.
- Two plan deviations accepted after review: P4's tombstone half is superseded by the equivalence
  invariant, and `STALE` falls to the same argument. Report: `docs/reports/slice-03-report.md`.

## Slice 3b — delivered

- **ADR-0006**: occurrence and observation identity is state-independent. An occurrence is a
  physical location fact; an observation is evidence about a file at a content hash. Neither
  depends on which run observed it. `content_hash` is the freshness anchor — already how
  `nerve why` worked, so product semantics are unchanged.
- The O(repository) state-restatement pass is **deleted**; `assertion_state` derivation and
  orphan pruning are scoped to what moved, with the whole-table versions kept as the oracle.
- **Slice 3's missed target is met: 24.9% → 2.0%** on the realistic corpus (orchestrator
  measured 2.0 / 2.0 / 2.5% at loads 5.3–9.2).
- **Counted-writes gate**: a one-file leaf edit writes the same 52 rows in a 100-file and a
  520-file repository. Deterministic, not timing-based, and cannot be gamed by a fast machine.
- Schema **v3**, with v1→v3 and v2→v3 migration tests plus an end-to-end check against a real
  v2 database produced by the Slice 3 binary.
- **Found and fixed a silent data-destruction bug**: only `nerve init` migrated, so a v3 binary
  indexing an un-migrated v2 database dropped every insert after deleting rows — 49 entities to
  33, exit code 0. Report: `docs/reports/slice-03b-report.md`.

## Slice 5 scope split (2026-07-31)

Row 5 was one row covering an ingestion path, a resolver with a precision gate, an invalidation
extension and a UI surface. It is split into **5a** (ingestion, structure, ADR status, T7) and
**5b** (link resolution, precision gate, surfacing) on the same seam as 2a/2b and 4a/4b, and for
the same recorded reason: a slice bundling two surfaces stalled an implementation agent, and the
same work split in two succeeded. Rationale, pushback and acceptance criteria:
`docs/plans/slice-05-document-evidence.md`.

Four points of pushback are recorded there and are binding on the implementation: the briefed
relation names (`DOCUMENT_CONTAINS_SECTION`, …) are rejected because Nerve's relation vocabulary is
endpoint-kind-agnostic; `ADR_DESCRIBES_COMPONENT` is refused as non-deterministic; "ADR status" is
a property, not an entity; and `tree-sitter-md` is rejected because it requires tree-sitter 0.26
against this workspace's 0.25.

## Slice 5a — delivered

- `.md`/`.markdown` discovered by the same rules as source; `md-structural 1.0.0` declaring
  `[DOCUMENT_STATED]` and nothing else; `Document` and `Section` entities with spans, content
  hashes and heading nesting; deterministic ADR recognition with a closed status vocabulary.
- **T7 satisfied by an exhaustive query, not a spot check**: no observation whose `file_path` is a
  document carries any source type but `DOCUMENT_STATED`. Mutation-verified — declaring
  `AST_DIRECT` makes the test fail and name all 54 offenders.
- **Two identity-forgery vectors closed.** The plan's `>`-joined `heading_path` was forgeable
  through printable text (found by the implementer); and `rel_path` — a field of *every* identity
  tuple — could carry a literal `0x1f`, which the orchestrator demonstrated merging two sections in
  two different files onto one entity. `canonical_child` now refuses the whole C0 range at the
  single path choke point, closing the class for every constructor, and counts the refusal.
- No dependency added. No schema migration. Report: `docs/reports/slice-05a-report.md`.

## Slice 5b — delivered, and an orchestrator decomposition error

Slice 5b was first dispatched as scanner **plus** resolver **plus** precision harness **plus**
invalidation. **The implementation agent stalled at the 600 s watchdog** — the second time this
project has hit that failure on an oversized slice, after `docs/CONTINUATION.md` had explicitly
warned about it. That is an orchestrator error and is recorded as one.

The partial work was inspected rather than discarded — it built, clippy was clean, the suite was
green, and the scanner was already wired in. The agent was resumed with a narrowed instruction
("test what you built, then stop"), which completed. Resolution became Slice 5c.

Delivered: link destinations recorded exactly as written, with syntax form and a span pointing at
the link; nothing normalized, resolved, stat-ed, opened or fetched. Bare inline-code identifiers are
counted, never emitted. Writing the tests found **six real defects**, including `</div>` and
`<script>alert(1)</script>` being recorded as link destinations — both from accepting a leading `/`
as "root-relative". Ambiguity is now a refusal. Report: `docs/reports/slice-05b-report.md`.

## Slice 5c — delivered

- `md-structural 1.1.0` emits `Section REFERENCES <File>`, and `REFERENCES <symbol>` where a
  `#L<n>` anchor resolves to the innermost covering span. Everything still carries
  `DOCUMENT_STATED`; only `directness` moves to `RESOLVED`. Verified across the whole real
  repository: **0** non-`DOCUMENT_STATED` observations on any `.md` path.
- Broken documentation links are first-class: `document_link_target_not_indexed`,
  `document_link_refused`, `document_anchor_no_symbol`. External destinations are counted, never
  fetched, and never entity-ised — **0** entities named `http…` or `javascript:…` on the real repo.
- **Fixture precision FP=0, recall 100% over 17 sites** — a regression gate, not an accuracy claim.
- **The finding that matters: Nerve's own 45 documents contain 5 Markdown link sites in total.**
  Real documentation refers to code in inline code spans, which this slice deliberately refuses to
  treat as links. High precision, narrow coverage, by design. Report:
  `docs/reports/slice-05c-report.md`.

## Slice 5d — why it exists

Indexing a documentation-only tree revealed 4 observations labelled `AST_DIRECT` in a repository
with **no TypeScript**: directory containment, attributed to `ts-js-structural`. `AST_DIRECT` means
"the syntax tree literally contains this relationship", and there is no syntax tree behind a
directory. This is the same defect class Slice 2a corrected for resolved imports. It is pre-existing
from Slice 1 and was made visible, not caused, by Slice 5a. The fix is a vocabulary addition plus a
UI gloss, so it is its own slice rather than a review amendment.

## Security gate

`docs/THREAT-MODEL.md` (2026-07-31) satisfies the `docs/SECURITY.md` requirement for a written
threat model. It specifies the blocking controls for the local HTTP surface (T4 CSRF/token +
DNS-rebinding, T5 XSS, T6 source serving), documents (T7), test evidence (T9) and MCP (T8).
It also raised one corrective item: there is no test asserting Nerve spawns no subprocess.

## Slice 4a — delivered

- New crate `nerve-server`: blocking loopback HTTP, worker pool, one `PRAGMA query_only`
  connection per worker. Nine read-only endpoints calling the same `nerve-store` functions the
  CLI uses — no business logic in the surface.
- **Tokio + axum rejected on measured evidence.** `tiny_http` costs **3** transitive crates
  against roughly 80–100 for the async stack, for a loopback-only single-user read-only server.
  93 → 100 crates total, all permissive, all recorded.
- **T4/T5/T6 implemented and attack-verified by the orchestrator**, not only by shipped tests:
  no token → 401, wrong token → 403, `Origin: evil.test` → 403, `Host: evil.test` → 403
  (DNS-rebinding), `POST` → 405, traversal and `/etc/passwd` → 403, a `.env` secret absent from
  every response, and an orchestrator-authored XSS payload escaped with **0 raw angle brackets**
  while still round-tripping losslessly.
- Read-only proven by sha256 before/after; clean SIGTERM shutdown leaving no database lock.
- One accepted risk recorded as threat-model **T11**: `tiny_http` reads header lines unbounded,
  a local availability issue with no disclosure path. Report: `docs/reports/slice-04a-report.md`.

## Slice 4b — delivered

- `apps/nerve-web`: React SPA compiled into the binary via `include_bytes!`. `nerve serve` needs
  no Node, no build step and no network. Runtime dependencies are **`react` + `react-dom` only**,
  lint-enforced; 67 build-time packages recorded as **not distributed**.
- Six views: Overview · Symbols · Entity (Relations / **Evidence** / Neighbourhood / Source) ·
  Gaps. The **evidence inspector is the centrepiece** — an assertion written as a sentence, each
  observation carrying extractor id+version and evidence type, and **freshness shown as the
  arithmetic it is**: recorded hash beside on-disk hash, with a verdict.
- The graph is always a **bounded neighbourhood**: on a 485-entity repository it draws 25 of 170
  and says `145 MORE NOT DRAWN`. Not a hairball (plan P4).
- **Fixed a Slice 4a bug that made the interface unloadable**: static assets required the session
  token, which a browser cannot attach to `<script src>`. Relaxed for the fixed asset table only;
  `Host` and `Origin` still enforced (they run *before* the token in `guard.check`), API routes
  and unknown paths still 401. Verified live.
- 31 screenshots reviewed across 4 repositories at 380px and 1600px; 0 CSP violations; database
  byte-identical before and after a UI session. Report: `docs/reports/slice-04b-report.md`.

## Slice 12 scope split, part two (2026-08-03)

Row 12 was already split into **12a** (object access) and **12b** (historical model) by
`docs/plans/slice-12-git-object-access-analysis.md`. Planning 12b split it again, into **12b**
(ingestion, storage, availability, two read commands) and **12c** (the derived questions and
every remaining surface). Same seam as 2a/2b, 5a/5b/5c, 6a/6b and 11a/11b, and the same recorded
reason: a slice bundling ingestion and a surface has cost this project five agents.

### The storage strategy, measured

The roadmap row required this to be measured rather than assumed. Measured on two real
repositories by `scripts/measure_history_storage.sh`, counting repository shape only:

| Repository | Commits | Files at HEAD | Snapshot path-rows | Delta change-rows | Ratio |
|---|---|---|---|---|---|
| Nerve | 85 | 420 | 22,958 | 762 | **30.1×** |
| A local 1,214-commit repository | 1,214 | 865 | 682,940 | 3,858 | **177×** |

**The ratio grows with history depth** — snapshot cost is `O(commits × tree_size)`, delta cost is
`O(total churn)` — so a single repository would have understated it. 682,940 rows exceeds the
entire current graph of any repository Nerve has indexed. Per-commit snapshots are rejected on
this evidence, along with selected-snapshots-plus-deltas and on-demand reconstruction: the delta
table answers every accepted question directly, so a reconstruction path would be cost with no
query behind it.

### Three positions the 12b plan takes against its own roadmap row

Recorded here because the row's wording will otherwise read as authoritative.

1. **"Commit entities" is declined.** History goes in dedicated tables; commits are not entities
   and changes are not assertions. Nerve has ruled on this question twice and stated the rule
   both times — `CoverageRun` *is* an entity because it had to be an **endpoint** of `COVERS`,
   and Slice 11a's `TraceRun` is *not* because it is **provenance**. A commit is provenance for a
   change fact whose subject is a *path*, and a historical path is not a current entity. The
   consequence is the argument: no entity kind, relation or source type is added, so
   `entity_fts`, selector resolution, `entities_total` and `ui_vocabulary.rs` are all untouched.
   Two qualifications came out of adversarial review of the plan: `documents.rs:331`'s
   migration-equivalence test **does** need a `DROP TABLE` rewind, because it re-migrates a
   physically-current database and `Step::Sql` would replay `CREATE TABLE`; and `symbols_total`
   was never at risk from a historical *File* entity, since `is_symbol()` covers only the four
   symbol kinds. The three invariants that are genuinely at risk are enough.
2. **Symbol-level history is refused in row 12**, with the cost stated rather than half-shipped.
   It needs a second identity namespace; without one it would break the three invariants above,
   one of which Slice 7a-iii was an entire corrective slice about.
3. **Historical impact is refused outright**, in the manner of `nerve affected`: it needs the
   historical graph, and computing it over the current graph would attribute today's edges to
   yesterday's commit.

### The invariant the slice exists to protect

A shallow boundary commit must never be diffed against the empty tree. Doing so reports every
file in the boundary tree as newly *added* at the boundary — "the project's history begins here",
stated as data rather than as prose. The boundary therefore carries
`changes_enumerated = 'parent_unavailable'` and **zero** change rows, and a mutation probe that
diffs it against the empty tree must fail a named test with the count of paths it wrongly
reported. `shallow_boundary`, `parents_missing`, `parents_unverifiable` and Nerve's own
`commit_budget` are four distinct reasons history stops. The third exists because adversarial
review found the boundary and a genuinely absent parent are **indistinguishable** in three
`store.rs` paths — an oversized or unreadable `.git/shallow` reads as *not shallow* — so the
four-way vocabulary would have reported a shallow repository as corrupt, the inverse of the error
the section warns about. The fourth, a bounded ingest, is the one the brief's list does not name
and the one that is Nerve's own doing.

### Two pre-existing defects the review surfaced

Both are fixed as part of 12b rather than deferred, because 12b cannot be correct without them.

1. **`discover::canonical_child` cannot guard a historical path.** It ends in
   `std::fs::canonicalize`, so it requires the path to *exist* — `coverage_ingest.rs:88-90`
   already documents the property. Routed through it, 12b would have refused every `deleted`
   change and made rename hypotheses structurally impossible, while counting each refusal as a
   path-safety success. A filesystem-free `discover::safe_tree_name` is added instead; the work is
   small because `gitobj::parse_tree` already refuses a per-entry name that is empty, contains
   `/`, or is `.` or `..`.
2. **`gitinfo::head_commit` does not follow `commondir`.** Measured on a real
   `git worktree add`: the worktree's HEAD names `refs/heads/feat`, which exists in neither the
   worktree's `refs/` nor its `packed-refs`. So it returns `None` — and its only production caller
   is `pipeline.rs:649`, which feeds `repository_state.git_commit`. **Indexing a linked worktree
   today records no commit for the state.** This is the failure 12a added `commondir` to prevent,
   reproduced one layer up in the ref reader instead of the object reader.
