# ADR-0003 — A status outside the vocabulary

**Status:** Mostly agreed, pending review

The vocabulary is closed. This value is not in it, so it is recorded as `unparsed` with the raw
text preserved — never coerced to the nearest member, which would invent a decision nobody made.

## Context

Text.
