# A note that lives among decisions

Recognised as an ADR by its directory rather than by its file name, so it carries no `adr_id`.
It states no status at all, and "absent" must not be reported as "unparsed".

## Context

Text.
