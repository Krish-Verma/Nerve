# ADR-0002 — Status in a section

## Status

Superseded

## Context

The second recognised form: the first non-empty line of a `## Status` section. It is consulted
only when no header line carries a status, so the two forms cannot disagree.
