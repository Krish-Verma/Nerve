# A guide whose file name contains a space

Indexed like any other document. It exists so that the percent-encoded destination in
`negatives.md` fails against a path that really is in the indexed set: `%20` naming nothing is a
decision about decoding, not an accident of the file being absent.
