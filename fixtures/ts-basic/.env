# Fixture only. Not a real credential. Excluded by the built-in secret deny-list before
# the file is ever read, so its contents never enter memory or the database.
EXAMPLE_TOKEN=not-a-real-secret-fixture-value
